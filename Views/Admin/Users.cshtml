@model IEnumerable<FitnessProje.Web.Models.AppUser>

@{
    ViewData["Title"] = "Kullanıcılar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-white fw-bold">👥 Kullanıcı Listesi</h2>
        <a asp-action="Index" class="btn btn-outline-warning">🔙 Panele Dön</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    <div class="card bg-dark border-secondary shadow-lg">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-dark table-hover mb-0 align-middle">
                    <thead class="bg-black text-warning">
                        <tr>
                            <th class="py-3 ps-4">Ad Soyad</th>
                            <th class="py-3">Email</th>
                            <th class="py-3 text-end pe-4">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (!Model.Any())
                        {
                            <tr>
                                <td colspan="3" class="text-center py-4 text-muted">
                                    Henüz kayıtlı kullanıcı bulunmuyor.
                                </td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var user in Model)
                            {
                                <tr>
                                    <td class="ps-4 fw-bold text-white">@user.FullName</td>
                                    <td class="text-secondary">@user.Email</td>
                                    <td class="text-end pe-4">
                                        <a asp-action="DeleteUser" asp-route-id="@user.Id" 
                                           class="btn btn-sm btn-outline-danger"
                                           onclick="return confirm('@user.FullName adlı kullanıcıyı silmek istediğinize emin misiniz?');">
                                            🗑️ Sil
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>