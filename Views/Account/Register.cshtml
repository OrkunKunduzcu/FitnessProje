@model FitnessProje.Web.ViewModels.RegisterViewModel

@{
    ViewData["Title"] = "Kayıt Ol";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card bg-dark border-warning shadow-lg">
                <div class="card-header bg-black border-bottom border-warning text-center py-3">
                    <h3 class="text-warning fw-bold mb-0">⚡ GYM-PRO Kayıt</h3>
                </div>
                
                <div class="card-body p-4">
                    <form asp-controller="Account" asp-action="Register" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label class="form-label text-white">Ad Soyad</label>
                            <input asp-for="FullName" class="form-control bg-secondary text-white border-0" placeholder="Örn: Ege Cinel" />
                            <span asp-validation-for="FullName" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label text-white">Email Adresi</label>
                            <input asp-for="Email" class="form-control bg-secondary text-white border-0" placeholder="mail@ornek.com" />
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label text-white">Şifre</label>
                            <input asp-for="Password" type="password" class="form-control bg-secondary text-white border-0" placeholder="******" />
                            <span asp-validation-for="Password" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label text-white">Şifre Tekrar</label>
                            <input asp-for="ConfirmPassword" type="password" class="form-control bg-secondary text-white border-0" placeholder="******" />
                            <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                        </div>

                        <div class="d-grid gap-2 mt-4">
                            <button type="submit" class="btn btn-warning fw-bold text-dark py-2">
                                🚀 Kaydı Tamamla
                            </button>
                        </div>

                        <div class="text-center mt-3">
                            <span class="text-muted">Zaten üye misin?</span>
                            <a asp-controller="Account" asp-action="Login" class="text-warning fw-bold ms-1">Giriş Yap</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>